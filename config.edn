{:app {:source-dir "/home/nas/proiecte/resources-cleanup"
       :target-dir "/data/target"}
 :jetty {:port 3000
         :join? false
         :h2c? true  ;; enable cleartext http/2
         :h2? true   ;; enable http/2
         :ssl? true  ;; ssl is required for http/2
         }}